import 'package:flutter/material.dart';

class HomeExpertScreen extends StatelessWidget {
  const HomeExpertScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Home - Expert')),
      );
}
