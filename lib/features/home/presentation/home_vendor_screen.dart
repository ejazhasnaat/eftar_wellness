import 'package:flutter/material.dart';

class HomeVendorScreen extends StatelessWidget {
  const HomeVendorScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Home - Vendor')),
      );
}
